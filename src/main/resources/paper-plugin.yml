name: VDiscordIntegration
version: "${version}"
main: de.varilx.discordIntegration.VDiscordIntegration
loader: de.varilx.discordIntegration.loader.VDiscordLoader
api-version: '1.21'
authors: [ Dario ]
description: A DiscordChatbridge, discord -> mc, mc -> discord
dependencies:
  server:
    LuckPerms:
      required: false
      load: BEFORE
      join-classpath: true
